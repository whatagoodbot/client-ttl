syntax = "proto3";
message UserDetails {
    string id = 1;
    string name = 2;
    string lastWelcomed = 3;
    string lastDisconnected = 4;
    bool admin = 5;
}
message UserRequest {
    string id = 1;
}

service Users {
  rpc getUserDetails (UserRequest) returns (UserDetails) {}
}
